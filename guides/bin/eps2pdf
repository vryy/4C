#!/bin/bash

# about : convert EPS to EPDF
# required : Perl
# date : 01.10.2003

infiles=$@

infile=$PWD/$1
outfile=$PWD/${1%*.*}.pdf
epstopdf_perl=epstopdf_perl

if [ -e $infile ]
then
    perl $epstopdf_perl --outfile=$outfile $infile
fi