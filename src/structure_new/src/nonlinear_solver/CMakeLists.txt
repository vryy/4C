baci_auto_define_module()

set(_structure_new_nln_solver_dependencies
    # cmake-format: sortable
    inpar
    )
baci_add_dependency(${AUTO_DEFINED_MODULE_NAME} ${_structure_new_nln_solver_dependencies})
